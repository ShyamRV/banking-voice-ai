[build]
builder = "nixpacks"

[build.nixpacksPlan.phases.setup]
cmds = ["pip install -r requirements-railway.txt"]

[deploy]
startCommand = "uvicorn api.dashboard_api:app --host 0.0.0.0 --port $PORT"
